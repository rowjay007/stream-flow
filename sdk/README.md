StreamFlow SDK (Go) — getting started

This directory will contain the official Go SDK for interacting with StreamFlow.

Goals:
- Provide typed client for producing/consuming records.
- Expose admin APIs for topic management and offsets.

Next steps:
- scaffold `sdk/client.go` with simple HTTP + gRPC client implementations.
